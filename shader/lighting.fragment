#ifdef GL_ES
    	precision highp float;
    #endif
	
	varying vec4 vColor;
	varying vec3 vLighting;
	
    void main(void) {
		//vec3 c=vColor * vLighting;
		//gl_FragColor = vColor;
        gl_FragColor = vec4(vColor.xyz * vLighting,vColor.a);
    }